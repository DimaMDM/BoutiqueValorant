<header class="bg-gray-800 shadow">
    <nav class="container mx-auto px-6 py-3">
        <div class="flex items-center justify-between">
            <div class="text-xl font-semibold">
                <a href="{{ path('app_home') }}" class="hover:text-gray-300">Boutique Valorant</a>
            </div>
            <div class="flex items-center">
                <a href="{{ path('app_skin_index') }}" class="px-3 py-2 rounded hover:bg-gray-700">Skins</a>
                <a href="{{ path('app_about') }}" class="px-3 py-2 rounded hover:bg-gray-700">À propos</a>
                <a href="{{ path('app_contact') }}" class="px-3 py-2 rounded hover:bg-gray-700">Contact</a>
                <div class="flex items-center ml-4">
                    <a href="{{ path('change_locale', {'locale': 'fr'}) }}" class="px-2">
                        <span role="img" aria-label="Français">🇫🇷</span>
                    </a>
                    <a href="{{ path('change_locale', {'locale': 'en'}) }}" class="px-2">
                        <span role="img" aria-label="English">🇬🇧</span>
                    </a>
                </div>
            </div>
        </div>
    </nav>
</header>
