{% extends 'base.html.twig' %}

{% block title %}Découvrez nos Skins{% endblock %}

{% block body %}
<body class="bg-gray-900 text-white">

    <!-- Main Content -->
    <main class="container mx-auto px-6 py-12">
        <div class="text-center mb-8">
            <h1 class="text-4xl font-bold mb-4">Découvrez nos Skins</h1>
            <p class="text-lg text-gray-400">Une sélection aléatoire de skins. Rafraîchissez pour en voir de nouveaux !</p>
            <a href="{{ path('app_skin_index') }}" class="mt-4 inline-block bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
                🔄 Rafraîchir
            </a>
        </div>

        <!-- Skins Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            {% for skin in skins %}
                <div class="bg-gray-800 rounded-lg shadow-lg overflow-hidden">
                    <img src="{{ asset('images/skins/' ~ skin.image) }}" alt="{{ skin.name }}" class="w-full h-48 object-cover">
                    <div class="p-6">
                        <h3 class="text-xl font-bold">{{ skin.name }}</h3>
                        <p class="text-gray-400">{{ skin.price }} VP</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 mt-12">
        <div class="container mx-auto px-6 py-4 text-center text-gray-400">
            &copy; {{ "now"|date("Y") }} Boutique Valorant. Tous droits réservés.
        </div>
    </footer>

</body>
{% endblock %}
